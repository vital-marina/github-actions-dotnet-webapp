name: WebApp MarinaVital

on:
  push:
   branch:
    - main
  schedule:
    - cron: '0 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    name: build

    steps:
      - name: Fazer checkou no repo
      - uses: actions/checkout@v4

      - name: Executar script
        run: echo "Esse é meu primeiro WF no GHA"